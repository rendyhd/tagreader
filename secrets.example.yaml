# Copy to secrets.yaml, which is ignored by Git. Never commit real credentials.
wifi_ssid: "YOUR_2_4_GHZ_WIFI"
wifi_password: "YOUR_WIFI_PASSWORD"
# Generate a fresh 32-byte base64 key, e.g. with: openssl rand -base64 32
# Or keep the existing key when migrating an already adopted ESPHome device.
movie_time_api_key: "REPLACE_WITH_YOUR_32_BYTE_BASE64_API_KEY"
movie_time_ota_password: "REPLACE_WITH_A_LONG_UNIQUE_PASSWORD"
